{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "test": { "dependsOn": ["^build"] },
    "typecheck": { "dependsOn": ["typecheck:e2e"] },
    "typecheck:e2e": {},
    "lint": {},
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**", ".next/**"] }
  }
}
